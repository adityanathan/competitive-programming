#include<bits/stdc++.h>
using namespace std;
#define forn(i,n) for(int i=0;i<n;i++)

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n,m;
	
	cin>>n>>m;
	vector<int> a(n),c(m);
	vector<pair<bool,int>happy(n);

	forn(i,n)
		cin>>a[i];

	forn(i,m)
		cin>>c[i];


	forn(i,n){
		if()
	}

	cout<<x<<"\n";

}