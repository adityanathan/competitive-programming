#pragma GCC optimize (Ofast)
#include<bits/stdc++.h>
#include<unordered_map>
using namespace std;
#define forn(i, n) for(int i = 0; i < n; ++i)
#define forit(it, m) for(auto it = m.begin(); it!=m.end(); ++it)
typedef pair<int, int> pi;

int main(){
	ios::sync_with_stdio(false);
  	cin.tie(0); 
	int t,n,test;
	cin>>test;
	t=test;
	while(t--){
		cin>>n;
		
		cout<<"Case #"<<test-t<<": "<<test<<"\n";
	    
	}
	return 0;
}